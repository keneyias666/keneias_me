services:
  - type: web
    name: portfolio
    env: python
    rootDir: portfolio
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: FLOWISE_API_URL
        sync: false
      - key: FLOWISE_API_KEY
        sync: false
